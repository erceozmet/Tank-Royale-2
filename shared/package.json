{
  "name": "@tank-royale/shared",
  "version": "1.0.0",
  "description": "Shared types and constants for Tank Royale",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
