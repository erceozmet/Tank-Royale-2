{
  "name": "tank-royale-load-tests",
  "version": "1.0.0",
  "description": "Load testing suite for Tank Royale 2",
  "scripts": {
    "setup": "node setup.js",
    "test:api": "artillery run api-load-test.yml",
    "test:websocket": "node websocket-load-test.js",
    "test:matchmaking": "node matchmaking-load-test.js",
    "test:all": "npm run setup && npm run test:api && npm run test:websocket && npm run test:matchmaking",
    "test:quick": "NUM_CLIENTS=50 NUM_PLAYERS=100 TEST_DURATION=30 npm run test:websocket && npm run test:matchmaking"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "socket.io-client": "^4.6.1"
  },
  "devDependencies": {
    "artillery": "^2.0.0"
  }
}
